version: '2'

services:
    tensorflow:
        build: .
        container_name: 'tensorflow-examples'
        env_file: .env
        volumes:
            - ./models:/code/models
        # command: bash -c "pip install -r requirements.txt && python manage.py collectstatic --no-input && python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
        # ports:
        #     - 8000:8000
